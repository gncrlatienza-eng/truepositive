# Docs

| Doc | Purpose |
|---|---|
| [`SPRINT_PLAN.md`](SPRINT_PLAN.md) | The active 8-week, 8-sprint agile build plan — sprint goals, tasks, acceptance criteria, cut list |

Additional spec documents (API reference, database schema, architecture deep-dive, deployment guides) will land here as each sprint produces real, implemented content — they're intentionally not stubbed out ahead of time to avoid documentation drifting from what's actually built.

The UI source of truth is an interactive mockup kept in `../reference/` (gitignored, not part of this repo) rather than a written spec.
